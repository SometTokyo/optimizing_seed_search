#ifndef RESEARCHALGO_BINARY_TERNARY_H
#define RESEARCHALGO_BINARY_TERNARY_H
#include <vector>
#include <string>
using namespace std;

int binary(const string &text, const vector<int> &suffixArray, char target);

int ternary(const string &text, const vector<int> &suffixArray, char target);

#endif //RESEARCHALGO_BINARY_TERNARY_H
